# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository layout

- `shift-hackathon.com/` — **active** site, hand-written Astro + React. See `shift-hackathon.com/CLAUDE.md` for how to work in it.
- `2026/` — static Framer/HTML export, reference only, do not hand-edit. Preview: `cd 2026 && docker run -it --rm -p 80:80 --name nginx -v $(pwd):/usr/share/nginx/html nginx`
